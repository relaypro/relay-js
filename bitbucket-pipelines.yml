#  relay-js CI

image: node:14.15.0

pipelines:
  default:
    - parallel:
        - step:
            name: Build and Test
            caches:
              - node
            script:
              - npm install
              - npm test
#        - step:
#            name: Code linting
#            script:
#              - npm install eslint
#              - npx eslint .
#            caches:
#              - node
